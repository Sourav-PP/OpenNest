export enum PlanBillingPeriod {
    MONTH = 'month',
    YEAR = 'year',
    WEEK = 'week'
}

export enum SubscriptionStatus {
    ACTIVE = 'active',
    PAST_DUE = 'past_due',
    CANCELED = 'canceled',
    TRIALING = 'trialing',
    UNPAID = 'unpaid',
    INACTIVE = 'inactive',
}
