export enum SortFilter {
    ASC = 'asc',
    DESC = 'desc',
}

export enum RevenueFilter {
    DAILY = 'daily',
    WEEKLY = 'weekly',
    MONTHLY = 'monthly',
}

export enum TopPsychologistSortFilter {
    CONSULTATION = 'consultation',
    RATING = 'rating',
    COMBINED = 'combined',
}
